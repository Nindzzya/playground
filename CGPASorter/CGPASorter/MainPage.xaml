<Page
    x:Class="CGPASorter.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CGPASorter"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Right" Margin="12" Grid.Column="0">
            <Button Content="Open Full List Source" Click="Button_Click"/>
            <TextBox x:Name="QueryBox" TextChanged="QueryBox_TextChanged"></TextBox>
            <ListView MaxHeight="400" x:Name="searchListView" ItemsSource="{x:Bind SearchListing}">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:Student">
                        <Grid MinWidth="250" MinHeight="42">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="42"></ColumnDefinition>
                            </Grid.ColumnDefinitions>
                            <TextBlock VerticalAlignment="Center" Grid.Column="0" Text="{x:Bind Name}"/>
                            <TextBlock VerticalAlignment="Center" Grid.Column="1" Text="{x:Bind CGPA}"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
        <ListView x:Name="FullListView" Grid.Column="1" ItemsSource="{x:Bind FullListing}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:Student">
                    <Grid MinWidth="250" MinHeight="42">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="42"></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <TextBlock VerticalAlignment="Center" Grid.Column="0" Text="{x:Bind Name}"/>
                        <TextBlock VerticalAlignment="Center" Grid.Column="1" Text="{x:Bind CGPA}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
