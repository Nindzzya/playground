<Page
    x:Class="iotX_Backend_Test.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:iotX_Backend_Test"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel>
            <TextBlock x:Name="statusBlock" Text="Not Ready"/>
            <ToggleSwitch x:Name="GPIO1" IsOn="{x:Bind Path=GPIOStatus[0], Mode=TwoWay}"/>
            <ToggleSwitch x:Name="GPIO2"/>
            <ToggleSwitch x:Name="GPIO3"/>
            <ToggleSwitch x:Name="GPIO4"/>
        </StackPanel>
    </Grid>
</Page>
